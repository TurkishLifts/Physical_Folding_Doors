return {
    CloseAngle = 0,
    CloseTime = 1,

    OpenAngle = 90,
    OpenTime = 1,
    OpenDelay = .05,

    CloseTolerance = 10,
    ToleranceDelay = 0.5,

    DoorType = "Quadruple", -- Quadruple|Double

    CustomSideConfig = {
    --[[
        ["back"] = {
            OpenTime = 3,
        }
    ]]--
    }
}